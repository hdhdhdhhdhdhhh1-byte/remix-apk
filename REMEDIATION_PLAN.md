# خطة الإصلاح والتطوير المستقبلي لمشروع Nico AI

بناءً على تقرير جاهزية الإنتاج (Production Readiness Audit)، تم تحديد عدة مجالات تتطلب اهتماماً فورياً لضمان إطلاق مستقر وفعال لمساعد Nico AI. تهدف هذه الخطة إلى معالجة المشاكل الحرجة، تحسين الأداء، تعزيز الأمان، وضمان التوافقية مع بيئات الإنتاج.

## 1. معالجة المشاكل الحرجة

### 1.1. تحويل الصوت إلى نص (STT) دون اتصال بالإنترنت
-   **المشكلة:** الاعتماد على نقطة نهاية خادم لتحويل الصوت إلى نص (`/api/nico/transcribe`) [1].
-   **الحل المقترح:**
    -   **البحث عن حلول STT محلية:** استكشاف مكتبات STT التي يمكن تشغيلها محلياً على الجهاز (مثل Vosk، Web Speech API مع نماذج دون اتصال، أو حلول Android/iOS الأصلية). يجب أن يكون الحل قادراً على دعم اللغة العربية بكفاءة.
    -   **إعادة تصميم `SpeechToText.ts`:** تعديل الكود لاستخدام المحرك المحلي الجديد، مع توفير خيار للعودة إلى الخدمة السحابية إذا كان الجهاز متصلاً بالإنترنت ولم يتم العثور على محرك محلي.

### 1.2. نظام الذاكرة الدائم والآمن
-   **المشكلة:** استخدام `localStorage` لتخزين الذاكرة الطويلة الأمد، مما يسبب مشاكل في الدوام، الأمان، الحجم، والبحث [2].
-   **الحل المقترح:**
    -   **اعتماد قاعدة بيانات محلية:** استبدال `localStorage` بقاعدة بيانات SQLite مدمجة (مثل `react-native-sqlite-storage` أو `sqflite` في Flutter إذا تم التحول) لتخزين الذاكرة الطويلة الأمد. هذا سيوفر دواماً أفضل، قدرة بحث أقوى، وإدارة أفضل للبيانات.
    -   **تشفير البيانات:** تطبيق تشفير للبيانات الحساسة المخزنة في قاعدة البيانات المحلية لتعزيز الأمان.
    -   **ترحيل المخطط (Schema Migration):** تنفيذ آلية لترحيل مخطط قاعدة البيانات لضمان التوافقية مع الإصدارات المستقبلية.
    -   **تحسين البحث:** استخدام قدرات SQLite المتقدمة للبحث (مثل FTS5) لتحسين كفاءة ودقة استرجاع الذاكرة.

### 1.3. توحيد نظام البناء (Build System Unification)
-   **المشكلة:** عدم اتساق مسارات الإخراج بين `capacitor.config.ts` و `.github/workflows/android-build.yml` [3, 4].
-   **الحل المقترح:**
    -   **توحيد مسار الإخراج:** تعديل `capacitor.config.ts` ليتوافق مع المسار المتوقع في سير عمل CI/CD، أو تعديل سير العمل ليتوافق مع `capacitor.config.ts` (الخيار الأول أفضل لتبسيط الإعدادات).

### 1.4. أتمتة عملية بناء Android
-   **المشكلة:** الحاجة إلى دمج يدوي لـ `AndroidManifest.additions.xml` [5].
-   **الحل المقترح:**
    -   **أتمتة دمج Manifest:** استكشاف أدوات أو سكربتات يمكنها دمج التغييرات المطلوبة في `AndroidManifest.xml` تلقائياً كجزء من عملية البناء، أو استخدام ميزات Capacitor المتقدمة لإدارة المكونات الإضافية.

### 1.5. تفعيل محرك كلمة التنبيه (Wake-Word Engine)
-   **المشكلة:** `VoiceBackgroundService.kt` يوفر هيكلاً بدون تطبيق فعلي لمحرك كلمة التنبيه [6].
-   **الحل المقترح:**
    -   **دمج محرك كلمة تنبيه محلي:** دمج مكتبة أو SDK لمحرك كلمة تنبيه يعمل دون اتصال بالإنترنت (مثل Picovoice Porcupine أو Snowboy) في `VoiceBackgroundService.kt`.
    -   **اختبار شامل:** إجراء اختبارات مكثفة لضمان عمل كلمة التنبيه بكفاءة ودقة في الخلفية وعلى مختلف أجهزة Android.

## 2. معالجة أخطاء TypeScript

### 2.1. `src/packages/core/NicoBrain.ts` - خطأ في نوع `decision`
-   **المشكلة:** عدم توافق أنواع حقل `decision` في `AgentTrace` [7, 8].
-   **الحل المقترح:**
    -   **توحيد أنواع `decision`:** التأكد من أن جميع مسارات الكود التي تقوم بتعيين قيمة لـ `trace.decision` تتوافق مع النوع `ReasoningDecision`. قد يتطلب ذلك إنشاء كائن `ReasoningDecision` وهمي أو مبسط للمسارات السريعة التي لا تمر عبر محرك الاستدلال الكامل.

### 2.2. `src/packages/knowledge/Database.ts` - ملف تعريف `sql.js` مفقود
-   **المشكلة:** عدم وجود ملف تعريف TypeScript لمكتبة `sql.js` [9].
-   **الحل المقترح:**
    -   **تثبيت `@types/sql.js`:** إضافة `npm install --save-dev @types/sql.js` إلى المشروع.

### 2.3. `src/packages/mobile-bridge/capacitor.ts` - عدم توافق أنواع `pickContact`
-   **المشكلة:** عدم توافق أنواع حقول `name` و `number` في `pickContact` [10, 11].
-   **الحل المقترح:**
    -   **تعديل أنواع الواجهة:** تعديل واجهة `PhoneActionsBridge` في `src/packages/mobile-bridge/types.ts` لتعكس بدقة الأنواع التي يمكن أن ترجعها `Contacts.pickContact`، أو تعديل تنفيذ `CapacitorPhoneActions` لضمان توافق الأنواع مع الواجهة الحالية.

## 3. تحسين توافقية Android/Termux

-   **الاعتماد على Web APIs:**
    -   **الحل:** حيثما أمكن، استبدال Web APIs بحلول أصلية (Native APIs) عبر Capacitor Plugins أو واجهات Android/iOS الأصلية. على سبيل المثال، استخدام حلول STT و TTS الأصلية بدلاً من تلك المعتمدة على المتصفح.
-   **التخزين المحلي غير الأصلي:**
    -   **الحل:** كما ذكر في قسم الذاكرة، التحول إلى SQLite أو حلول تخزين أصلية أخرى.

## 4. تنظيف الملفات غير الضرورية

-   **الحل المقترح:**
    -   **حذف `KnowledgeEngine.backup.ts`:** هذا الملف هو نسخة احتياطية ويجب حذفه من الكود المصدري.
    -   **نقل `seed-large.ts` و `test-engine.ts`:** نقل هذه الملفات إلى مجلد `tools/` أو `scripts/` أو `__tests__/` خارج مسار بناء الإنتاج.

## 5. تحسينات الأداء

-   **معالجة الصوت في الذاكرة:**
    -   **الحل:** إعادة تصميم `encodeWav` أو مكون `SpeechToText` لمعالجة تدفقات الصوت بشكل متزايد (streaming) بدلاً من تخزين جميع الأجزاء في الذاكرة. يمكن استخدام Web Workers أو حلول أصلية لمعالجة الصوت في الخلفية.
-   **البحث في الذاكرة الطويلة الأمد:**
    -   **الحل:** بعد التحول إلى SQLite، استخدام فهارس Full-Text Search (FTS5) لتحسين أداء البحث بشكل كبير في `LongTermMemory`.
-   **الاستعلامات الأولية في KnowledgeEngine:**
    -   **الحل:** إذا كانت قاعدة البيانات كبيرة جداً، يمكن تحسين الاستعلامات الأولية باستخدام FTS5 مباشرة في SQLite، مما يقلل من الحاجة لتحميل عدد كبير من المرشحين إلى الذاكرة لإجراء حسابات `Cosine Similarity`.

## 6. معالجة عوائق الإنتاج

-   **عدم وجود اختبارات شاملة:**
    -   **الحل:** كتابة اختبارات وحدة (Unit Tests) واختبارات تكامل (Integration Tests) شاملة لـ `KnowledgeEngine`، `NicoBrain`، `MemoryManager`، وجميع المكونات الحرجة الأخرى. استخدام إطار عمل مثل `Vitest` أو `Jest`.
-   **غياب التوثيق الشامل:**
    -   **الحل:** إنشاء وثائق مفصلة تغطي:
        -   هندسة النظام (System Architecture).
        -   عمليات النشر (Deployment Procedures) لـ Android والويب.
        -   دليل المطور (Developer Guide) للمساهمين الجدد.
        -   دليل استكشاف الأخطاء وإصلاحها (Troubleshooting Guide).
-   **الأمان:**
    -   **الحل:** إجراء تدقيق أمني شامل. تطبيق التشفير للبيانات الحساسة في التخزين المحلي. مراجعة جميع نقاط النهاية (endpoints) لضمان عدم وجود ثغرات أمنية.
-   **قابلية التوسع:**
    -   **الحل:** إعادة تقييم بنية النظام لضمان قابلية التوسع. على سبيل المثال، فصل الخدمات إلى وحدات أصغر (microservices) إذا كان هناك جزء سيتم تشغيله على الخادم.
-   **مراقبة الأخطاء والتسجيل:**
    -   **الحل:** دمج مكتبة تسجيل قوية (مثل Winston أو Pino) لتسجيل الأحداث والأخطاء. استخدام أداة لمراقبة الأخطاء في الإنتاج (مثل Sentry أو Bugsnag) لتتبع المشكلات في الوقت الفعلي.

## المراجع

[1] `src/packages/voice/SpeechToText.ts` - السطر 37
[2] `src/packages/memory/LongTermMemory.ts` - السطر 19، 58
[3] `.github/workflows/android-build.yml` - السطر 38
[4] `capacitor.config.ts` - السطر 10
[5] `apps/mobile/android/AndroidManifest.additions.xml` - السطر 1-4
[6] `apps/mobile/android/VoiceBackgroundService.kt` - السطر 36-39
[7] `src/packages/core/NicoBrain.ts` - خطأ TypeScript في السطر 196
[8] `src/packages/shared/agent.ts` - السطر 107
[9] `src/packages/knowledge/Database.ts` - خطأ TypeScript في السطر 1
[10] `src/packages/mobile-bridge/capacitor.ts` - خطأ TypeScript في السطر 149
[11] `src/packages/mobile-bridge/types.ts` - السطر 28
[12] `src/packages/voice/SpeechToText.ts` - السطر 42-45
[13] `src/packages/voice/SpeechToText.ts` - السطر 128-135
[14] `src/packages/knowledge/KnowledgeEngine.ts` - السطر 109
[15] `ls -R /home/ubuntu/remix-apk/src/packages/knowledge/__tests__` - لا يوجد مجلد اختبارات
